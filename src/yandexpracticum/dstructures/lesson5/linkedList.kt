package yandexpracticum.dstructures.lesson5

/**
 * Задачи:
 * 1) дана голова односвязного списка, в каждом элементе которого
 *    записано целое положительное число. Нужно удалить все узлы
 *    с нечётными значениями
 *
 * 2) дана голова односвязного списка, нужно поменять узлы местами так,
 *    чтобы значения шли в порядке неубывания. Проще говоря,
 *    нужно отсортировать список, не копируя элементы.
 */